# Reproducibility Package
## Explainable Cyber-Physical Fusion Detection and Physics-Feasible Adversarial Robustness for Smart Grids

All results in the paper are regenerated by the 20 scripts below (fixed seeds throughout).
Run order matters only for Dataset 1: run `build_features.py` first — every Dataset-1 script reads its cached output (`results/features_ds1.parquet`).

## Setup

```
pip install -r requirements.txt
```

Datasets (not redistributed here; see paper Sec. 4 for sources):
- Dataset 1: SmartGrid-CyberPhysical-Attack-Dataset → place under `Dataset/`
- Dataset 3: IEC-104/61850 traces (Matousek et al.) → place under `data/`
- Testbed 2: IEEE-14/118 are generated in-script via pandapower (no download).

## Script → paper result map

| Script | Paper result |
|---|---|
| `build_features.py` | Builds the 1,529 fused windows (Sec. 4, Table 1). **Run first.** |
| `cyber_physical_fusion.py` | Physical vs cyber vs fusion (Table 2, top rows) |
| `ornl_baseline_shap.py` | SHAP feature importances (Fig. SHAP) |
| `exp1_blocked_cv.py` | Shuffled vs time-blocked CV vs temporal hold-out (Table 2, Fig. protocols) |
| `exp8_rolling_origin.py` | Rolling-origin temporal CV, 0.88±0.06 (Table 2, Fig. rolling-origin) |
| `exp2_gnn_headtohead.py` | Topology-free GCN baseline comparison (Table 2, Fig. GCN) |
| `advanced_experiments.py` | RF vs GB vs MLP; PGD-transfer attack (Table 2 bottom row, Table 4) |
| `adversarial_robustness.py` | Graded attacks, adversarial training, smoothing (Table 4, Fig. spectrum) |
| `selective_detection.py` | Risk–coverage / selective abstention (Tables 3 & 6) |
| `exp3_feasible_advtrain.py`, `exp3_run.py` | IEEE-14 feasible FDI, ES evasion 0.26→0.53 (Results II, Figs.) |
| `iec104_crossval.py`, `iec104_hmi_crossval.py` | Real IEC-104 behavioral attacks (Results III, Table 5) |
| `exp4_semantic_fusion.py` | Observability taxonomy (Results IV, Table 7, Fig. taxonomy) |
| `exp5_ac_stealth_temporal.py` | AC WLS estimator, stealthy FDI vs BDD, temporal test (Results V, Table 8) |
| `exp5_submission.py` | Ramp-rate sweep, temporal detector, 50 trials/point (Table 9) |
| `exp6_manifold_detector.py` | Rate-invariant manifold test (Results V, Fig. manifold) |
| `exp6_submission.py` | Manifold/fused detector sweep at matched FPR (Table 9, Fig.) |
| `exp7_ieee118.py`, `exp7_bench.py` | IEEE-118 analytical extrapolation + real BDD solve (Fig. IEEE-118) |

### Revision scripts (added for the revised manuscript)

| Script | Paper result |
|---|---|
| `exp4b_taxonomy_ci.py` | Observability taxonomy over 20 seeds with Wilson 95% CIs; FPR recalibration (Table of Results IV) |
| `exp6b_ksweep.py` | Manifold-dimension K sensitivity sweep, K=1..27 (defends the K=4 choice; Fig. K-sweep) |
| `exp3b_testbed2_ci.py` | Testbed-2 feasible-FDI, 15 repeated 70/30 splits, macro-F1 CI (Results II error bars) |
| `exp7b_ieee118_partial.py` | Partial real per-timestep AC-WLS IEEE-118 run under attack (upgrades the extrapolation) |

Outputs (figures `.png`, numbers `.txt`/`.json`) are written to `results/`.

## Notes

- Random seeds are fixed in every script (RNG=42 unless stated in-script).
- Detector-comparison thresholds are calibrated on benign streams to a common 5% false-alarm rate; measured rates are reported in the paper's table captions.
- The IEEE-118 detection curves use the consistent-injection identity (analytical extrapolation); the single real AC WLS BDD solve is in `exp7_ieee118.py`. See paper Results V for the stated assumptions.
